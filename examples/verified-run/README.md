# Verified Run Sample

This compact sample was captured from live run `20260716T090956234937Z`: DataHub Lite v1.6.0 storage through the official `mcp-server-datahub==0.6.0` process.

It shows the exact DataHub URNs and MCP result hashes, the minimal patch, the regression failure and success, and the verified tag write-back. The complete runtime bundle also contains the generated test and a SHA-256 manifest.

Run `make gate-live` to generate a fresh complete bundle under `artifacts/runs/<run-id>/` and update the root `EVIDENCE.md`.
